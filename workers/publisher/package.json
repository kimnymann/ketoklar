{
  "name": "ketoklar-publisher",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test-publish-local": "curl -X POST http://localhost:8787/publish -H \"x-publish-secret: test\""
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260901.1",
    "wrangler": "^4.20.0"
  }
}
